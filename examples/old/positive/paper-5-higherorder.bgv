# Example from our paper, section 2.5

let
  higher_order : !(Chan (?Int. return)). Close -[ u 0 ]->
                 ?Int. ?String. Close -[ p 1 ]->
                 Unit
  higher_order h c =
    send (&c : ?Int. Return) &h;
    close h;
    let s = "" + (recv &c) in
    close c
in

unit
